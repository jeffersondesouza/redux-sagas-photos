import React, { Component } from 'react';
import { Link } from 'react-router-dom';

import './styles.css';

const SiteHome = () => (
  <div className="site-home">
    <h2 className="site-home__title">Sagas Photos home!</h2>
  </div>
);

export default SiteHome;
